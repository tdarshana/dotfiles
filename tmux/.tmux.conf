# ============================================
# Theme Specific Settings
# ============================================

# Dracula theme for Tmux =====================
set -g @plugin 'dracula/tmux'
# Dracula Theme Settings
set -g @dracula-battery-colors "dark_gray dark_purple"
set -g @dracula-border-contrast true
set -g @dracula-cpu-usage-label " "
set -g @dracula-fixed-location "Singapore"
set -g @dracula-git-colors "dark_purple green"
set -g @dracula-git-no-repo-message ''
set -g @dracula-git-no-untracked-files true
set -g @dracula-git-show-current-symbol ✓
set -g @dracula-git-show-diff-symbol !
set -g @dracula-git-show-remote-status true
set -g @dracula-military-time true
set -g @dracula-network-bandwidth wlan0
set -g @dracula-network-bandwidth-interval 0
set -g @dracula-network-bandwidth-show-interface true
set -g @dracula-plugins "git time battery"
set -g @dracula-ram-usage-label "󰍜 "
set -g @dracula-refresh-rate 5
set -g @dracula-show-empty-plugins true
set -g @dracula-show-flags true
set -g @dracula-show-left-icon "session"
set -g @dracula-show-left-sep ""
set -g @dracula-show-powerline true
set -g @dracula-show-right-sep ""
set -g @dracula-time-colors "gray dark_purple"
set -g @dracula-time-format "%F %R"

# Nord Theme for Tmux =====================
# set -g @plugin "nordtheme/tmux"

# # Gruvbox theme for Tmux =====================
# set -g @plugin 'egel/tmux-gruvbox'
# set -g @tmux-gruvbox 'dark'

# set -g @tmux-gruvbox-statusbar-alpha 'true'
# set -g @tmux-gruvbox-left-status-a '#S' # tmux's session name

# set -g @tmux-gruvbox-right-status-x '%m/%d/%Y' # e.g.: 01/31/2024
# set -g @tmux-gruvbox-right-status-y '%I:%M %p' # 09:54 PM
# set -g @tmux-gruvbox-right-status-z '#h #{tmux_mode_indicator}'

# TokyoNight theme for Tmux =====================
# set -g @plugin 'fabioluciano/tmux-tokyo-night'
# # TokyoNight Theme Settings
# set -g @theme_variation 'storm'
# set -g @theme_active_pane_border_style "#737aa2"
# set -g @theme_inactive_pane_border_style "#292e42"
# set -g @theme_window_with_activity_style "italics"
# set -g @theme_status_bell_style "bold"
# set -g @theme_plugins "datetime,weather,battery"
# set -g @theme_left_separator ''
# set -g @theme_right_separator ''
# set -g @theme_transparent_status_bar 'true'
# set -g @theme_transparent_left_separator_inverse ''
# set -g @theme_transparent_right_separator_inverse ''
# #TokyoNight theme plugins - datetime
# set -g @theme_plugin_datetime_icon ""
# set -g @theme_plugin_datetime_accent_color ""
# set -g @theme_plugin_datetime_accent_color_icon ""
# set -g @theme_plugin_datetime_format ""
# #TokyoNight theme plugins - weather
# set -g @theme_plugin_weather_icon ""
# set -g @theme_plugin_weather_accent_color ""
# set -g @theme_plugin_weather_accent_color_icon ""
# set -g @theme_plugin_weather_format ""
# set -g @theme_plugin_weather_location ""
# #TokyoNight theme plugins - battery
# set -g @theme_plugin_battery_charging_icon ""
# set -g @theme_plugin_battery_discharging ""
# set -g @theme_plugin_battery_red_threshold ""
# set -g @theme_plugin_battery_yellow_threshold ""
# set -g @theme_plugin_battery_red_accent_color ""
# set -g @theme_plugin_battery_red_accent_color_icon ""
# set -g @theme_plugin_battery_yellow_accent_color ""
# set -g @theme_plugin_battery_yellow_accent_color_icon ""
# set -g @theme_plugin_battery_green_accent_color ""
# set -g @theme_plugin_battery_green_accent_color_icon ""

# TokyoNight theme 2 for Tmux =====================
# set -g @plugin "janoamaral/tokyo-night-tmux"
# # TokyoNight Theme 2 Settings
# set -g @tokyo-night-tmux_theme storm    # storm | day | default to 'night'
# set -g @tokyo-night-tmux_transparent 1  # 1 or 0
# # Number styles
# set -g @tokyo-night-tmux_window_id_style digital
# set -g @tokyo-night-tmux_pane_id_style hsquare
# set -g @tokyo-night-tmux_zoom_id_style dsquare
# # Icon styles
# set -g @tokyo-night-tmux_terminal_icon 
# set -g @tokyo-night-tmux_active_terminal_icon 
# # No extra spaces between icons
# set -g @tokyo-night-tmux_window_tidy_icons 0
# # Widget - Show datetime
# set -g @tokyo-night-tmux_show_datetime 1
# set -g @tokyo-night-tmux_date_format YMD
# set -g @tokyo-night-tmux_time_format 24H
# # Netspeed widget
# set -g @tokyo-night-tmux_show_netspeed 1
# set -g @tokyo-night-tmux_netspeed_iface "wlan0" # Detected via default route
# set -g @tokyo-night-tmux_netspeed_showip 1      # Display IPv4 address (default 0)
# set -g @tokyo-night-tmux_netspeed_refresh 1     # Update interval in seconds (default 1)
# # Path Widget
# set -g @tokyo-night-tmux_show_path 1
# set -g @tokyo-night-tmux_path_format relative # 'relative' or 'full'
# # Battery Widget
# set -g @tokyo-night-tmux_show_battery_widget 1
# set -g @tokyo-night-tmux_battery_name "BAT1"  # some linux distro have 'BAT0'
# set -g @tokyo-night-tmux_battery_low_threshold 21 # default

# Kanagawa theme for Tmux =====================
# set -g @plugin 'Nybkox/tmux-kanagawa'

# # Kanagawa Theme Settings
# set -g @kanagawa-theme 'wave'
# set -g @kanagawa-ignore-window-colors true # preserve your emulator background / foreground
# set -g @kanagawa-show-powerline true
# set -g @kanagawa-show-left-sep 
# set -g @kanagawa-show-right-sep 
# set -g @kanagawa-plugins "cpu-usage gpu-usage ram-usage"
# set -g @kanagawa-show-empty-plugins false
# set -g @kanagawa-show-flags true
# # the default is 5, it can accept any number
# set -g @kanagawa-refresh-rate 5
# set -g @kanagawa-show-left-icon session
# set -g @kanagawa-left-icon-padding 1
# set -g @kanagawa-cpu-usage-label "CPU" # Customize label
# set -g @kanagawa-cpu-display-load true # Show system load average instead of CPU usage percentage (default)
# set -g @kanagawa-battery-label "Battery" # Customize label
# set -g @kanagawa-gpu-usage-label "GPU"  # GPU usage label
# set -g @kanagawa-ram-usage-label "RAM"  # RAM usage label
# set -g @kanagawa-tmux-ram-usage-label "MEM" # RAM usage by TMux label
# # network-bandwidth
# set -g @kanagawa-network-bandwidth wlan0
# set -g @kanagawa-network-bandwidth-interval 0
# set -g @kanagawa-network-bandwidth-show-interface true
# # ssh-session options
# set -g @kanagawa-show-ssh-session-port true
# # time options
# set -g @kanagawa-show-timezone false
# set -g @kanagawa-day-month true
# set -g @kanagawa-military-time true
# set -g @kanagawa-time-format "%F %R"
# # git options
# set -g @kanagawa-git-disable-status true # Hide details of git changes
# set -g @kanagawa-git-show-current-symbol ✓ # Set symbol to use for when branch is up to date with HEAD - default is ✓. Avoid using non unicode characters that bash uses like $, * and !
# set -g @kanagawa-git-show-diff-symbol !  # Set symbol to use for when branch diverges from HEAD - default is unicode !. Avoid bash special characters
# set -g @kanagawa-git-no-repo-message "" # Set symbol or message to use when the current pane has no git repo - default is unicode no message
# set -g @kanagawa-git-no-untracked-files true # Hide untracked files from being displayed as local changes - default is false
# set -g @kanagawa-git-show-remote-status true # Show remote tracking branch together with diverge/sync state - default is false
# # weather options
# set -g @kanagawa-show-fahrenheit false # Switch from default fahrenheit to celsius
# set -g @kanagawa-fixed-location "Some City" # Set a fixed location for weather
# set -g @kanagawa-show-location false # Hide your location
# # continuum options
# # Set the output mode. Options are:
# #   countdown: Show a T- countdown to the next save (default)
# #   time: Show the time since the last save
# #   alert: Hide output if no save has been performed recently
# #   interval: Show the continuum save interval
# set -g @kanagawa-continuum-mode time
# # Show if the last save was performed less than 60 seconds ago (default threshold is 15 seconds)
# set -g @kanagawa-continuum-time-threshold 60

# Catppuccin Theme  for Tmux =====================
# set -g @plugin 'catppuccin/tmux'
# # Catppuccin Theme Settings
# set -g @catppuccin_flavor "mocha"
# set -g @catppuccin_window_status_style "rounded"

# # Make the status line pretty and add some modules
# set -g status-right-length 100
# set -g status-left-length 100
# set -g status-left ""
# set -g status-right "#{E:@catppuccin_status_application}"
# set -agF status-right "#{E:@catppuccin_status_cpu}"
# set -ag status-right "#{E:@catppuccin_status_session}"
# set -ag status-right "#{E:@catppuccin_status_uptime}"
# set -agF status-right "#{E:@catppuccin_status_battery}"

# Theme Specific Settings end ==================

# ============================================
# Tmux Plugins with TPM

# Vim Navagations for TMux
set -g @plugin 'christoomey/vim-tmux-navigator'
# Tmux Which Key by <Super> + Space
set -g @plugin 'alexwforsythe/tmux-which-key'
# Restore sessions even after a system restart
set -g @plugin 'tmux-plugins/tmux-resurrect'
# Auto save tmux Environment
set -g @plugin 'tmux-plugins/tmux-continuum'

# ============================================

# General Configuration Options
set-option -g activity-action other
set-option -g assume-paste-time 1
set-option -g base-index 0
set-option -g bell-action any
set-option -g default-command ''
set-option -g default-size 80x24
set-option -g destroy-unattached off
set-option -g detach-on-destroy on
set-option -g display-panes-active-colour red
set-option -g display-panes-colour blue
set-option -g display-panes-time 1000
set-option -g display-time 4000
set-option -g history-limit 50000
set-option -g key-table root
set-option -g lock-after-time 0
set-option -g lock-command "lock -np"
set-option -g message-command-style bg=black,fg=yellow
set-option -g message-line 0
set-option -g message-style "bg=#44475a,fg=#f8f8f2"
set-option -g mouse off
set-option -g prefix C-s
set-option -g prefix2 None
set-option -g renumber-windows off
set-option -g repeat-time 500
set-option -g set-titles off
set-option -g set-titles-string "#S:#I:#W - \"#T\" #{session_alerts}"
set-option -g silence-action other

# Status Bar Settings - default theme
# set-option -g status-format[0] "#[align=left range=left #{E:status-left-style}]#[push-default]#{T;=/#{status-left-length}:status-left}#[pop-default]#[norange default]#[list=on align=#{status-justify}]#[list=left-marker]<#[list=right-marker]>#[list=on]#{W:#[range=window|#{window_index} #{E:window-status-style}#{?#{&&:#{window_last_flag},#{!=:#{E:window-status-last-style},default}}, #{E:window-status-last-style},}#{?#{&&:#{window_bell_flag},#{!=:#{E:window-status-bell-style},default}}, #{E:window-status-bell-style},#{?#{&&:#{||:#{window_activity_flag},#{window_silence_flag}},#{!=:#{E:window-status-activity-style},default}}, #{E:window-status-activity-style},}}]#[push-default]#{T:window-status-format}#[pop-default]#[norange default]#{?window_end_flag,,#{window-status-separator}},#[range=window|#{window_index} list=focus #{?#{!=:#{E:window-status-current-style},default},#{E:window-status-current-style},#{E:window-status-style}}#{?#{&&:#{window_last_flag},#{!=:#{E:window-status-last-style},default}}, #{E:window-status-last-style},}#{?#{&&:#{window-bell-flag},#{!=:#{E:window-status-bell-style},default}}, #{E:window-status-bell-style},#{?#{&&:#{||:#{window_activity_flag},#{window_silence_flag}},#{!=:#{E:window-status-activity-style},default}}, #{E:window-status-activity-style},}}]#[push-default]#{T:window-status-current-format}#[pop-default]#[norange list=on default]#{?window_end_flag,,#{window-status-separator}}}#[nolist align=right range=right #{E:status-right-style}]#[push-default]#{T;=/#{status-right-length}:status-right}#[pop-default]#[norange default]"
# set-option -g status-format[1] "#[align=centre]#{P:#{?pane_active,#[reverse],}#{pane_index}[#{pane_width}x#{pane_height}]#[default] }"
# set-option -g status-interval 5
# set-option -g status-justify left
# set-option -g status-keys emacs
# set-option -g status-left "#[bg=#50fa7b,fg=#282a36]#{?client_prefix,#[bg=#f1fa8c],} #S  #[fg=#50fa7b,bg=#44475a]#{?client_prefix,#[fg=#f1fa8c],}"
# set-option -g status-left-length 100
# set-option -g status-left-style default
# set-option -g status-position bottom
# set-option -g status-right "#{?#{==:#(~/.dotfiles/tmux/.tmux/plugins/tmux/scripts/git.sh),},,#[fg=#6272a4,nobold,nounderscore,noitalics]#[fg=#50fa7b,bg=#6272a4] #(~/.dotfiles/tmux/.tmux/plugins/tmux/scripts/git.sh) }#{?#{==:%F %R,},,#[fg=#44475a,nobold,nounderscore,noitalics]#[fg=#6272a4,bg=#44475a] %F %R }#{?#{==:#(~/.dotfiles/tmux/.tmux/plugins/tmux/scripts/battery.sh),},,#[fg=#282a36,nobold,nounderscore,noitalics]#[fg=#6272a4,bg=#282a36] #(~/.dotfiles/tmux/.tmux/plugins/tmux/scripts/battery.sh) }"
# set-option -g status-right-length 250
# set-option -g status-right-style default
# set-option -g status-style "bg=#44475a,fg=#f8f8f2"

# Status Bar Settings
set-option -g status-position bottom
set-option -g status on

# Update Environment Variables
set-option -ga update-environment "DISPLAY"
set-option -ga update-environment "KRB5CCNAME"
set-option -ga update-environment "SSH_ASKPASS"
set-option -ga update-environment "SSH_AUTH_SOCK"
set-option -ga update-environment "SSH_AGENT_PID"
set-option -ga update-environment "SSH_CONNECTION"
set-option -ga update-environment "WINDOWID"
set-option -ga update-environment "XAUTHORITY"

# Visual and Sound Settings
set-option -g visual-activity off
set-option -g visual-bell off
set-option -g visual-silence off

# Word Separators for Pane Navigation
set-option -g word-separators "!\"#$%&'()*+,-./:;<=>?@[\\]^`{|}~"

# Key Bindings
set-option -g prefix C-s
unbind C-b  # Unbind the default prefix (Ctrl-b)

# Default Shell
set-option -g default-shell /opt/homebrew/bin/fish

# TPM Run
run -b '~/.dotfiles/tmux/.tmux/plugins/tpm/tpm'
